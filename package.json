{
  "name": "vestiges-public-site",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "node --check script.js && node --check theme-init.js && node --check forge-viewer.js && node --check worker/src/index.mjs && node --check worker/src/validation.mjs",
    "build": "node scripts/validate-site.mjs",
    "test": "node --test tests/*.test.mjs worker/test/*.test.mjs",
    "validate": "npm run lint && npm run build && npm test"
  }
}
